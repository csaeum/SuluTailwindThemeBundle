{# Text + Images block — classic side-by-side layout #}
{% extends '@ItechWorldSuluTheme/blocks/common/_block_wrapper.html.twig' %}

{% block block_content %}
    {% include '@ItechWorldSuluTheme/blocks/common/_titles.html.twig' with {
        title: title|default(''),
        subTitle: subTitle|default('')
    } only %}

    {% set position = imagePosition|default('right') %}
    {% set formatMap = {'16:9': 'iw_theme_16_9', '4:3': 'iw_theme_4_3', '1:1': 'iw_theme_1_1', '3:4': 'iw_theme_3_4', 'original': ''} %}
    {% set format = formatMap[imageFilter|default('original')]|default('') %}

    <div class="grid grid-cols-1 md:grid-cols-2 gap-8 items-center {{ position == 'left' ? 'md:flex-row-reverse' : '' }}">
        {# Text column #}
        <div class="{{ position == 'left' ? 'md:order-2' : 'md:order-1' }}">
            {% include '@ItechWorldSuluTheme/blocks/common/_paragraph.html.twig' with {
                text: text|default('')
            } only %}
        </div>

        {# Images column #}
        <div class="{{ position == 'left' ? 'md:order-1' : 'md:order-2' }}">
            {% if images is defined and images.ids is defined %}
                {% for media in images.ids %}
                    {% include '@ItechWorldSuluTheme/blocks/common/_image.html.twig' with {
                        media: sulu_resolve_media(media, app.request.locale),
                        imageFormat: format,
                        showName: showImagesName|default(false)
                    } only %}
                {% endfor %}
            {% endif %}
        </div>
    </div>
{% endblock %}
